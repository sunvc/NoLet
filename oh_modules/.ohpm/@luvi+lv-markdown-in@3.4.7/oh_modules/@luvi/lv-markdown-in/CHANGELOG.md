### ArkTS 工业级鸿蒙原生 Markdown 渲染解决方案

---

### 版本更新描述

v3.4.7版本

1. 修复 `setTextSelectionActionItems()` 配置多个自定义按钮后，文本选择操作栏可能超出屏幕边界的问题
2. 文本选择操作栏根据按钮数量和 Markdown 可视宽度自动调整位置，超宽时支持横向滚动
3. 新增 `clearTextSelection()` 接口，支持业务主动清除选区高亮、拖拽手柄和内部操作栏
4. 修复代码块无法参与 Markdown 统一文本选择的问题，代码内容支持与其他文本块连续选取
5. 修复行内 LaTeX 公式与金额文本的解析冲突，闭合的 `$...$` 数字公式正常解析，未闭合金额保持原文且不占用后续公式分隔符
6. 修复普通文本中间的裸 URL 无法识别、查询参数被截断以及尾随标点进入链接的问题
7. 修复数字 HTML 实体可能造成文本损坏，以及 `<font>`、带颜色 `<span>` 位于段落中间时无法生成样式节点的问题

---

v3.4.6版本

1. 新增 `setTableCopyClickListener()` 接口，设置表格复制按钮点击监听，点击后返回当前表格的原始 Markdown
2. 新增 `setTableDownloadClickListener()` 接口，设置表格下载按钮点击监听，点击后返回当前表格的原始 Markdown
3. 新增 `setTableExpandClickListener()` 接口，设置表格放大按钮点击监听，点击后返回当前表格的原始 Markdown
4. 新增 `setTableActionBackgroundColor()` 接口，支持自定义表格操作区背景色
5. 新增 `setTableActionIconColor()` 接口，支持统一设置表格复制、下载和放大图标的滤镜颜色
6. 优化表格操作区圆角和外边框，使操作区与表格形成统一容器
7. 修复窄表格启用操作区后被拉伸至屏幕宽度的问题
8. 修复异常表格列数不一致或表头连续下划线时可能触发的生命周期闪退
9. 表宽缓存改为基于 AST 节点的 WeakMap，避免多实例、嵌套表格和内容更新时缓存碰撞
10. 新增 `setCodeBlockActionAreaVisible()` 接口，支持隐藏普通代码块整个顶部操作区

---

v3.4.5版本

1. 优化流式渲染时插件闪烁问题

---

v3.4.4版本

1. 新增代码块自动折叠能力，超过阈值的代码块可默认折叠展示
2. 新增 `setCodeBlockAutoCollapseEnable()` 接口，设置是否开启代码块自动折叠
3. 新增 `setCodeBlockAutoCollapseThreshold()` 接口，设置代码块自动折叠阈值
4. 新增 HTML 代码块预览能力，支持将 `html` / `htm` 代码块渲染为预览内容
5. 新增 `setHtmlPreviewEnable()` 接口，设置是否开启 HTML 代码块预览
6. 新增 `setHtmlPreviewJavaScriptEnable()` 接口，设置 HTML 预览是否允许执行 JavaScript
7. 新增 ArkWeb 预览池能力，优化 Mermaid / HTML 预览频繁创建 Web 组件的性能开销
8. 新增 `setWebPreviewPoolEnable()` 接口，设置是否开启 ArkWeb 预览池
9. 新增 `setWebPreviewPoolSize()` 接口，设置 ArkWeb 预览池大小
10. 新增 `setTextSelectionActionItems()` 接口，支持自定义跨 Text 文本选择后的操作栏按钮
11. 优化懒渲染能力，开启 `setLazyRender(true)` 后离开可见区域的内容支持回收，降低长文档内存占用
12. 优化流式输入时 Mermaid / HTML 代码块预览逻辑，代码块闭合后再切换为预览渲染
13. 优化代码块流式渲染时图标闪动的问题
14. 优化代码块流式输出时若开启自动不折叠时不折叠，输出结束再折叠
15. 修复连续 HTML `<p>` 标签解析后段落挤在一起的问题
16. 修复标题内超链接文本字号未继承标题字号的问题
17. 优化代码块流式渲染时图标闪动的问题
18. 修复列表内块级 LaTeX 公式解析异常

---

v3.4.3版本

1. 支持 Mermaid 图表渲染
2. 新增 `setMermaidEnable()` 接口，设置是否开启 mermaid 渲染
3. 新增 `setMermaidTheme()` 接口，设置 Mermaid 深浅色主题
4. 新增 `setMermaidSecurityLevel()` 接口，设置 Mermaid 预览安全级别
5. 新增 `setQuoteTextColor()` 接口，设置引用块文本颜色
6. 修复渲染出来的任务列表可交互的问题

---

v3.4.2版本

1. 新增 `setLazyRender()` 接口，支持设置是否开启可见区域懒渲染，优化大文档加载性能
2. 新增 `setLazyPreloadBlockCount()` 接口，设置懒渲染预渲染块数量
3. 新增 `setSetextHeadingEnable()` 接口，支持设置是否开启 Setext 标题解析
4. 新增 `setTableTitleTextColor()` 接口，支持设置表格表头文本颜色
5. 新增 `setTableInterleaveTextColor()` 接口，支持设置表格错行文本颜色
6. 新增 `setTableNormalTextColor()` 接口，设置表格非错行文本颜色
7. 修复 $ 符号解析金额时异常的问题

---

v3.4.1版本

1. 支持 Setext 标题语法
2. 修复部分复杂超链接无法被正确解析的问题
3. 修复在禁用文本复制时抢占外部 .gesture(LongPressGesture()) 的问题

---

v3.4.0版本

1. 新增实例级 `plugins` 配置扩展自定义语法，并将匹配结果渲染为对应组件。
2. 新增 `setLatexClickListener()` 接口，支持在公式图片渲染完成后获取公式原文与 `pixelMap`
3. 新增对 `<font color size face>` 行内快速样式的解析支持
4. 新增跨 Text 文本选择能力，支持长按拖拽选择并通过监听回调交由业务侧执行复制
5. 新增 `setTextSelectionEnable()` 接口，设置是否开启组件的长按选择能力，默认关闭；开启后，可通过长按 Markdown
   文本并拖拽两侧光标的方式选择跨段落内容。
6. 新增 `setTextSelectionCopyListener()` 接口，设置文本选择后的复制监听。因系统剪贴板涉及业务权限控制，组件只回传最终选中的文本内容，由业务侧决定是否写入剪贴板。
7. 修复模块名非 entry 时，渲染 LaTeX 公式时，公式显示为乱码的问题
8. 弃用 `setCopyOption()` 接口

---

v3.3.1版本

1. 优化流式数据输入时表格闪屏的问题
2. 优化锚点识别逻辑并遵循相关规范
3. 修复子线程渲染时部分组件数据未渲染的问题
4. 子线程加载能力重构，多Markdown组件线程共享一个线程，提升性能
5. 新增特殊符号解析能力
6. 新增超链接自动解析能力
7. 修复有序列表序号折行的问题
8. 修复表格中渲染数学公式异常问题

---

v3.3.0版本

1. 优化流式数据输入时表格闪屏的问题
2. 修复列表中表格解析不正确的问题
3. 新增块级元素间距接口 setBlockSpacing
4. 新增引用块内边距接口 setQuotePadding

---

v3.2.9版本

1. 修改表格内无法正确解析公式的问题

---

v3.2.8版本

1. 修复普通文本无法正常换行的问题

---

v3.2.7版本

1. 修复表格文本强制换行时无法正常解析表格的问题
2. 修复普通文本无法正常换行的问题

---

v3.2.6版本

1. 新增锚点超链接信息获取能力
2. 新增图片尺寸自定义能力

---

v3.2.5版本

1. 新增设置剪贴板复制范围选项接口 setCopyOption

---

v3.2.4版本

1. 新增标题自定义字体接口 setTitleFontFamily
2. 新增普通文本自定义字体接口 setTextFontFamily

---

v3.2.3版本

1. 新增子线程加载控制接口 **setThreadRenderEnable**，支持禁用子线程加载。
2. 优化组件销毁时及时释放创建的线程
3. 修复表格项中无数据时单元格没渲染的问题

---

v3.2.2版本

1. 优化标题字体颜色接口 **setTitleColor**，支持给指定级别的标题设置字体颜色
2. 新增自定义样式接口：
    - 设置`标题`字体粗细 **setTitleFontWeight**，支持给指定级别的标题设置字体粗细
    - 设置`标题`字体样式 **setTitleFontStyle**，支持给指定级别的标题设置字体样式
    - 设置`引用块`圆角 **setQuoteBorderRadius**
    - 设置`引用块`左边宽度 **setQuoteBorderWidth**
    - 设置`代码块`圆角 **setCodeBlockBorderRadius**
    - 设置`图片`圆角 **setImageBorderRadius**
    - 设置`表格`圆角 **setTableBorderRadius**
    - 设置`表格`外边框颜色 **setTableOuterBorderColor**
    - 设置`表格`外边框宽度 **setTableOuterBorderWidth**
    - 设置`表格`内边框颜色 **setTableInnerBorderColor**
    - 设置`表格`内边框宽度 **setTableInnerBorderWidth**
    - 设置`表格`屏幕对齐方式 **setTableAlign**
    - 设置`超链接`字号 **setHyperlinkTextSize**
    - 设置`无序列表`指示点大小 **setUlPointSize**
    - 设置`无序列表`指示点颜色 **setUlPointColor**

---

v3.2.0版本

1. 增加线程动态保活机制，避免线程超限

---

v3.1.9版本

1. 修复数学公式渲染模糊的问题

---

v3.1.8版本

1. 新增 Worker 线程解析资源，优化解析效率
2. 修复公式解析与金额解析冲突的问题
3. 修复其他已知问题

---

v3.1.6版本

1. 修复把“纯 Markdown”当“HTML”去做了 htmlToMarkdown() 转换的问题，感谢开源贡献者：[jimmy54](https://gitee.com/jimmy54)
   提交的issue
2. 段落模块增加文本复制

---

v3.1.5版本

1. 修复已知问题

---

v3.1.4版本

1. 完善数学公式解析
2. 修复已知问题

---

v3.1.3版本

1. 修复已知问题

---

v3.1.2版本

1. 解决C2通道回传敏感行为

---

v3.1.1版本

1. 因隐私政策调整，移除代码块的复制文本到剪贴板的能力
2. 增加 setCodeCopyListener() 接口注册代码块复制监听

---

v3.1.0版本

1. 修复 text 解析闪退问题
2. 签名 har 包
3. 增加网络权限声明
4. 增加隐私政策

---

v3.0.9版本

1. 新增行内代码背景圆角设置，默认值 5vp
2. 新增超链接背景色设置，默认值 Color.Transparent
3. 新增超链接背景圆角设置，默认值 5vp

---

v3.0.8版本

1. 修复已知闪退问题

---

v3.0.7版本

1. 修改为字节码方式打包

---

v3.0.6版本

1. 修复列表缩进导致的闪退问题

---

v3.0.5版本

1. 修复销毁markdownController导致闪退的问题

---

v3.0.4版本

1. 修复流式输入内容时闪屏的问题

---

v3.0.3版本

1. 修复设置代码块主题闪退的问题

---

v3.0.2版本

1. 新增 MarkdownController
2. 新增 Markdown 组件，弃用 ~~LvMarkdownIn~~ 组件，您需使用最新的 Markdown 组件，详细可查看文档中 **升级指南** 段落。
3. 新增图片代理
4. 使用模态框加载网页
5. 深色模式兼容
6. 重写图片，超链接点击监听
7. 图片预览支持缩放
8. 性能优化
9. UI 优化
10. 修复已知问题

---

v3.0.1版本

1. 文档描述修改

---

v3.0.0版本

1. 底层重构，样式自定义更简便
2. 新增图片加载代理
3. 优化嵌套语法解析
4. 完善 readme 接口描述
5. UI 调整
6. 修复已知问题

---

v2.0.15版本

1. 修复列表特定加粗格式下，无法正常显示的问题
2. 修复代码块内容较长，无法滑动至最右边的问题

---

v2.0.13版本

1. 修复表格的表头和标题宽度没有对齐的问题

---

v2.0.12版本

1. 修复列表文字溢出的问题

---

v2.0.11版本

1. 禁用单选按钮

---

v2.0.10版本

1. 增加 LvImage 的导出
2. 优化表格展示效果

---

v2.0.9版本

1. 修复标题字体颜色无法修改的问题
2. 修复无脚注的情况下，页面下方默认边距问题

---

v2.0.8版本

1. 修复标题嵌套使用不解析其他语法的问题

---

v2.0.7版本

1. 无关键更新

---

v2.0.6版本

1. 修复 text 纯文本加载模式闪退问题

---

v2.0.5版本

1. 新增支持 html 常用标签解析
2. 优化无序列表展示样式

---

v2.0.4版本

1. 无关键更新

---

v2.0.3版本

1. 增加脚注语法的支持
2. 优化列表与引用关联使用间的样式问题
3. 优化代码着色的显示效果
4. 优化表格与字体相关的支持
5. 优化各语法组合使用的逻辑

---

v2.0.2版本 (milepost)

1. 新增 3 种加载模式：text、rawfile、sandbox
2. 新增 lvText 行高设置属性
3. 新增 lvCode 代码块索引展示控制方法
4. 新增代码块超10行自动折叠
5. 修复同行链接识别异常的问题
6. 修复列表解析出现的问题
7. 优化相关性能及样式问题
8. 图片默认宽度由 "60%" 调整为 "200vp"

---

v2.0.1版本 增加遗忘的未导出的mdRegister方法

v2.0.0版本 适配api10及以上，增加适配图像点击、超链接点击回调方法，可自定义控制跳转行为、取消默认的List组件的滚动条

v1.1.1版本 修复双星号加粗文本的bug

v1.1.0版本 增加img与font标签的解析支持，修复url解析相关问题

v1.0.9版本 暂无更新描述

v1.0.8版本 增加图片点击预览，修复列表解析对齐问题，增加web组件加载提示

v1.0.7版本 修复数据响应式问题

v1.0.6版本 readme描述调整

v1.0.5版本 增加列表、复选框解析

v1.0.3版本 暂无更新描述
